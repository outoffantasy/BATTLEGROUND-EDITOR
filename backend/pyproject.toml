[project]
name = "battleground-editor-backend"
version = "0.1.0"
description = "Data processing scripts for the Battleground editor."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "pre-commit>=4.0.0",
]

[tool.uv]
package = false
